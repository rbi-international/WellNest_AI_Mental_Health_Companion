version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: wellnest-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: wellnest
    volumes:
      - mongo_data:/data/db
    restart: always

volumes:
  mongo_data: